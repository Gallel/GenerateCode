
public class HeapSort<E> extends Object implements SortingAlgorithm<E> {

    // Default constructor
    public HeapSort() {
    }
    
    // Constructor that takes a vector of type E as input
    public HeapSort(E[] vector) {
    }

    // Method to sort a sequence of items using HeapSort and an integer n
    @Override
    public void sort(int n) {
        // code to implement HeapSort with priority queue
    }

    // Method to sort a sequence of items using HeapSort and a vector of type E and an integer n
    @Override
    public void sort(E[] vector, int n) {
        // code to implement HeapSort with priority queue
    }
}
