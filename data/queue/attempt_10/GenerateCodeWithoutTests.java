
public class QueueArrayImpl<E> extends Object implements Queue<E>, FiniteContainer<E> {

    //constructor without parameters
    public QueueArrayImpl() {
        //code to initialize the queue with default maximum capacity
    }

    //constructor with maximum capacity parameter
    public QueueArrayImpl(int max) {
        //code to initialize the queue with the given maximum capacity
    }

    //method to add an element to the queue
    public void add(E elem) {
        //code to add element to the end of the queue
    }

    //method to check if the queue is empty
    public boolean isEmpty() {
        //code to check if the queue is empty
    }

    //method to check if the queue is full
    public boolean isFull() {
        //code to check if the queue is full
    }

    //method to retrieve the first element in the queue
    public E peek() {
        //code to retrieve the first element in the queue
    }

    //method to delete the first element in the queue
    public E poll() {
        //code to delete the first element in the queue and return it
    }

    //method to retrieve the size of the queue
    public int size() {
        //code to retrieve the size of the queue
    }

    //method to overwrite Object.toString()
    public String toString() {
        //code to return a string representation of the queue
    }

    //method to retrieve an iterator over the elements in the queue
    public Iterator<E> values() {
        //code to return an iterator over the elements in the queue
    }
}
